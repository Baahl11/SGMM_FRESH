# Estado real de la implementación V2 al 2026-08-26

Producido en respuesta al Bloque 0 de `HANDOFF_V2.1_ADENDA_CORRECTIVA_RECEPCION_IA.md`. Cero código tocado para este reporte — es puro grep/read contra el estado actual del repo en `main` (commit `83e29009` al momento de escribir esto), verificado archivo por archivo, no de memoria.

---

## Entregable 0.A — Estado real por fase

| Fase V2 | Estado | Evidencia | Commits |
|---|---|---|---|
| Fase 0 — Seguridad y esquema | **completada** | `docs/reception-ai/fase-0-report.md`; ver detalle de hallazgos A-1/A-2/A-3/A-4/A-15 abajo | `9a1829b4`..`b8952697` |
| Fase 1 — Consolidación mensajería | **completada** (alcance acotado — ver nota) | `docs/reception-ai/fase-1-report.md` | `fc823af1`..`b6231003`, más `83e29009` (fix post-merge no relacionado) |
| Fase 2 — Ingestión idempotente / sombra | **no_iniciada** | cero migraciones `reception_*`, `app/api/webhooks/whatsapp/route.ts` no tocado | — |
| Fase 3 — FAQ e inbox | **no_iniciada** | — | — |
| Fase 4 — Motor de reservas | **no_iniciada** | — | — |

**Nota sobre "completada" sobre Fase 1:** la Fase 1 del V2 se completó en su alcance declarado (adaptador Meta + facade de credenciales + una ruta piloto migrada + fix de concurrencia del worker), documentado explícitamente en su propio spec como estructural y sin tráfico real. No incluyó tocar el webhook, el guardado de configuración, ni la otra ruta de envío legacy — eso quedó explícitamente fuera de alcance y coincide con varios de los hallazgos nuevos de esta adenda (A-9 a A-14, A-17, A-20 viven todos en código que Fase 1 nunca tocó).

---

## Entregable 0.B — Respuestas con evidencia

**1. ¿Existe `lib/messaging/adapters/meta-whatsapp.ts`? ¿Cableado o solo declarado?**
Existe y **sí está cableado**, pero solo a una ruta: `app/api/messaging/whatsapp/send/route.ts` (vía `MetaWhatsAppAdapter` + `lib/messaging/provider-service.ts`). `createAdapter()` lo registra para el worker (`lib/workers/messaging-worker.ts`) también, aunque el worker nunca recibe jobs `meta_whatsapp` reales todavía (nadie escribe en `messaging_providers`). **No** está cableado al webhook (`app/api/webhooks/whatsapp/route.ts:549` sigue con `fetch()` directo y `v18.0` hardcodeado) ni a `app/api/whatsapp/validate-config/route.ts:50` (mismo hardcode).

**2. ¿Existen las tablas `reception_*`? ¿Se aplicaron en algún entorno?**
No existen. Cero archivos de migración `reception_*` en `supabase/migrations/`. Fase 2 no ha empezado.

**3. ¿Existe sistema de feature flags?**
Confirmado con grep sobre `app/`, `lib/`, `supabase/`: **cero coincidencias** de `feature_flag`. Coincide exactamente con lo que dice la adenda — no existe, hay que construirlo.

**4. ¿`app/api/webhooks/whatsapp/route.ts` sigue ejecutando el LLM en línea, o ya se partió?**
Sigue siendo la misma ruta monolítica sin partir. No se tocó en Fase 0 ni Fase 1 (estaba fuera de alcance de ambas). Sigue devolviendo `500` en su catch final (línea ~522) y sigue sin consultar `webhook_events` para idempotencia — confirmado A-9 abierto.

**5. ¿Se rotaron las credenciales de Twilio de `.env.whatsapp.example`?**
Esto ya se investigó a fondo en Fase 0, con una conclusión más fuerte que la de esta adenda: no solo son de demostración — **nunca existió una credencial de Twilio real y viva para este proyecto**, verificado por tres vías independientes: (a) `git log --all` muestra que el archivo con el valor "con forma real" nunca estuvo en ningún commit anterior al que ya lo reemplazó por placeholders; (b) se revisaron los `.zip` de respaldo pre-Fase-0 del repo y ninguno tiene una copia con el valor real; (c) el operador confirmó directamente en la consola de Twilio (captura de "API keys & tokens") que la cuenta no tiene ninguna API key creada. Los placeholders explícitos ya están en el archivo desde Fase 0 (commit `0f7a7c34`, previo a este reporte). Documentado en `docs/reception-ai/secret-rotation-plan.md`.

**6. ¿Se obtuvo el dump de esquema de producción y el diff contra local?**
No se hizo un `supabase db dump`/`db diff` formal — bloqueado por falta de Docker Desktop en esta máquina (ambos subcomandos lo requieren). En su lugar se hizo una verificación equivalente de solo lectura: consultas directas contra `information_schema` del proyecto real (`SGMM-PRO`, ref `sbwpqtrxhiuucwlbozet`) comparando las 68 tablas y 3 funciones de seguridad/RLS que definen las migraciones locales contra el esquema real. **Resultado: sin deriva de esquema real** — las migraciones que el CLI marcaba como "solo local" sí estaban aplicadas en producción, solo nunca quedaron registradas en la tabla de historial del CLI. Documentado con metodología completa en `docs/reception-ai/schema-reconciliation.md`. No es un dump/diff con la herramienta que pide la adenda, pero es evidencia real contra el esquema real, no una suposición.

**7. ¿Ya se tocó `app/api/whatsapp/validate-config/route.ts`?**
Sí, en Fase 0: ahora exige `getAuthUser()` + rate limit por usuario (`lib/security/rate-limit.ts`). Cierra A-2 de esta adenda. **Sigue pendiente**, sin embargo, A-18 en este mismo archivo: la versión de Graph API sigue hardcodeada como `v18.0` en la línea 50, no usa la constante centralizada que Fase 1 creó en el adaptador nuevo.

**8. ¿Hay algún cambio que este documento vaya a pisar?**
No se detectó ninguno — ver Entregable 0.C.

---

## Entregable 0.C — Conflictos

**Ninguno detectado entre esta adenda y lo ya construido.** La adenda es aditiva y consistente con las decisiones ya tomadas en Fase 0/Fase 1 (mismo patrón de DTOs seguros sin exponer tokens, mismo enfoque de adaptador por proveedor, mismo orden secuencial de fases). Ningún hallazgo A-n pide deshacer código que ya escribimos.

Dos puntos abiertos al momento de escribir esta sección, **cerrados el mismo día** con la conformidad del propietario:

- **`BITACORA.md` y `DECISIONES.md`** — no existían en el repo. Creados en `docs/reception-ai/` (mismo directorio que el resto de la documentación operativa de este proyecto). `DECISIONES.md` ya trae D-1/D-2/D-3 del Bloque 1 de la adenda y la nota de Fase 6 del Bloque 2.4, con fecha y fuente.
- **3.1, confirmación de credenciales Twilio ficticias** — cerrado en `BITACORA.md`, entrada del 2026-08-26, con la nota de precisión de qué exactamente se verificó y qué no (la página de "API keys", no la vista principal de Account SID/Auth Token).

---

## Hallazgos de la adenda verificados contra el código actual (no de memoria)

### Ya cerrados por Fase 0/Fase 1

| Hallazgo | Estado real | Evidencia |
|---|---|---|
| A-1 (log de token) | ✅ Cerrado | `app/api/user/whatsapp-settings/route.ts:129` — comentario confirma que el `console.log` de `updateData` fue eliminado |
| A-2 (validate-config sin auth) | ✅ Cerrado | `app/api/whatsapp/validate-config/route.ts:2-18` — `getAuthUser()` + `checkRateLimit()` presentes |
| A-4 (`select('*')` en messaging/config) | ✅ Cerrado | `app/api/messaging/config/route.ts:46,152,166` — usa `SAFE_COLUMNS`, no `select('*')` |
| A-15 (tabla `whatsapp_conversations` fuera de `supabase/migrations`) | ✅ Cerrado | migración canónica `20260824120000_whatsapp_conversations.sql` creada en Fase 0; confirmado además que la tabla existe de verdad en `SGMM-PRO` (no solo que la migración es segura) vía `schema-reconciliation.md` |
| A-16 (worker sin claim atómico) | ✅ Cerrado | `lib/workers/messaging-worker.ts:95-108` — `claimJob()` con `UPDATE...WHERE status='pending'...RETURNING` |

### Parcialmente cerrados — requieren trabajo adicional

| Hallazgo | Estado real | Qué falta |
|---|---|---|
| A-3 (token leído desde el navegador) | ⚠️ Parcial | El código cliente ya no lo lee (`page.tsx` usa el endpoint server-side desde Fase 0) — **pero la policy de RLS sigue permitiéndolo**. Verificado en `supabase/migrations/20250120_user_profiles.sql:39-42`: `"Users can view their own profile" FOR SELECT USING (auth.uid() = user_id)` es una policy a nivel de fila, no de columna — RLS de Postgres no puede restringir columnas individuales con una policy simple. Cualquiera con sesión válida puede seguir pidiendo `whatsapp_access_token` directo desde la consola del navegador contra su propia fila, y la policy `"Admins can view all profiles"` (línea 50-59) extiende eso a cualquier fila si el rol `admin` está ampliamente otorgado. La adenda tenía razón: quitar la llamada no cierra el agujero. |
| A-18 (Graph API `v18.0` hardcodeada) | ⚠️ Parcial | Centralizada como `GRAPH_API_VERSION` en `lib/messaging/adapters/meta-whatsapp.ts:3`, pero **las dos ubicaciones legacy siguen hardcodeadas**: `app/api/webhooks/whatsapp/route.ts:549` y `app/api/whatsapp/validate-config/route.ts:50` — ninguna de las dos fue tocada por Fase 1 (fuera de su alcance declarado). |

### Confirmados abiertos, sin tocar (coinciden exactamente con la adenda)

Verificado con grep/read directo, no asumido: A-5 (patrón `T00:00:00` presente tal cual en `lib/calendar-utils.ts:96,178` y `app/api/public/availability/[slug]/route.ts:28,160`), A-6 (`getDay()` en `check-availability/route.ts:23` vs. `dayNames` asumiendo `0=Lunes` en `webhooks/whatsapp/route.ts:234-236` — la inconsistencia es real y está exactamente donde la adenda dice), A-7 (`app/api/public/availability/[slug]/route.ts:148-162` filtra solo por `clinic_user_id`/`user_id`, cero `doctor_id`), A-8 (cero `EXCLUDE`/`btree_gist` en todas las migraciones), A-9 (webhook sigue devolviendo 500), A-19 (cero feature flags), A-20 (cero referencias a `patient_whatsapp_consent`/`opted_out` en el webhook).

No verifiqué línea por línea A-10, A-11, A-12, A-13, A-14 ni A-17 para este reporte — ninguno vive en código que Fase 0 o Fase 1 haya tocado, así que no hay razón para dudar del hallazgo original de la adenda; los verificaré con evidencia fresca justo antes de tocarlos, cuando lleguemos a la fase que les corresponde (Bloque 4 de la adenda).

---

## Kill-rule del Bloque 0

Cumplida: este reporte se produjo con evidencia de commits reales (`git log`) y lectura directa del código actual, no de memoria de sesión ni de lo que decía el handoff original.
